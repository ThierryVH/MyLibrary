{{ form_start(form) }}
{{ form_row(form.title) }}

    {{ form_row(form.author) }}
{{ form_row(form.release_date) }}
{{ form_row(form.summary) }}
{{ form_row(form.category) }}





<p>Image :</p>
<div class="row">
    <div class="col-3">
    {{ form_row(form.image.name)}}
    </div>
    <div class="col-5">
{% if book.image.name is defined %}
<img width="80" src="{{ asset('uploads/images/') ~ book.img.name }}">
{% endif %}
    
    </div>
    </div>
{{ form_row(form.image.alt) }}


    <input type="submit" value="{{ submit|default('Save') }}">
{{ form_end(form) }}
